services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env.production
    environment:
      NODE_ENV: production
      PORT: ${PORT:-3000}
    ports:
      - "3000:3000"
